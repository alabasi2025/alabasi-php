# 📘 دليل التثبيت السريع
## نظام السندات والقيود الذكية

---

## 🎯 الخطوة 1: تحميل الملفات

قم بتحميل ملف `smart-vouchers-system.zip` وفك الضغط عنه.

---

## 🎯 الخطوة 2: رفع الملفات إلى XAMPP

انسخ جميع الملفات إلى مجلد النظام:
```
D:\AAAAAA\xampp\htdocs\alabasi\
```

### الملفات التي سيتم رفعها:
- ✅ `install_smart_vouchers.sql`
- ✅ `install-smart-vouchers.php`
- ✅ `receipt-vouchers.php`
- ✅ `payment-vouchers.php`
- ✅ `api/receipt-vouchers.php`
- ✅ `api/payment-vouchers.php`
- ✅ `api/journals.php`
- ✅ `js/receipt-vouchers.js`
- ✅ `js/payment-vouchers.js`

---

## 🎯 الخطوة 3: تثبيت قاعدة البيانات

### الطريقة الأولى: التثبيت التلقائي (موصى به)

1. افتح المتصفح
2. انتقل إلى:
   ```
   http://localhost/alabasi/install-smart-vouchers.php
   ```
3. انتظر حتى تظهر رسالة "✅ تم التثبيت بنجاح!"
4. اضغط على أحد الأزرار للانتقال إلى النظام

### الطريقة الثانية: التثبيت اليدوي

1. افتح phpMyAdmin: `http://localhost/phpmyadmin`
2. اختر قاعدة البيانات `alabasi_unified`
3. اضغط على تبويب "SQL"
4. انسخ محتوى ملف `install_smart_vouchers.sql` والصقه
5. اضغط "تنفيذ"

---

## 🎯 الخطوة 4: التحقق من التثبيت

بعد التثبيت، تحقق من إنشاء الجداول التالية:

| الجدول | الوصف | عدد الأعمدة |
|--------|-------|-------------|
| `receipt_vouchers` | سندات القبض | 17 |
| `payment_vouchers` | سندات الصرف | 17 |
| `journal_details` | تفاصيل القيود | 8 |
| `voucher_sequences` | أرقام السندات | 4 |

---

## 🎯 الخطوة 5: البدء في الاستخدام

### سندات القبض 📥
```
http://localhost/alabasi/receipt-vouchers.php
```

### سندات الصرف 📤
```
http://localhost/alabasi/payment-vouchers.php
```

### القيود اليومية 📝
```
http://localhost/alabasi/journals.php
```

---

## 🎯 الخطوة 6: إنشاء أول سند

### مثال: سند قبض

1. افتح صفحة سندات القبض
2. اضغط "➕ سند قبض جديد"
3. املأ البيانات:
   - **التاريخ:** اليوم
   - **المستلم من:** اسم العميل
   - **المبلغ:** 1000
   - **طريقة الدفع:** نقداً
   - **الحساب المدين:** الصندوق
   - **الحساب الدائن:** المبيعات
4. اضغط "✅ حفظ وترحيل"

**النتيجة:**
- ✅ تم إنشاء سند القبض برقم تلقائي (RV-2025-00001)
- ✅ تم إنشاء قيد محاسبي تلقائياً
- ✅ تم ربط السند بالقيد

---

## ⚠️ استكشاف الأخطاء

### خطأ: "لا يمكن الاتصال بقاعدة البيانات"
**الحل:**
1. تأكد من تشغيل XAMPP (Apache + MySQL)
2. تحقق من إعدادات قاعدة البيانات في `includes/db.php`

### خطأ: "الجدول غير موجود"
**الحل:**
1. قم بتشغيل `install-smart-vouchers.php` مرة أخرى
2. أو نفذ ملف `install_smart_vouchers.sql` يدوياً

### خطأ: "القيد غير متوازن"
**الحل:**
- تأكد من أن مجموع المدين = مجموع الدائن

---

## 📞 الدعم الفني

في حالة وجود أي مشاكل:

1. **تحقق من سجل الأخطاء:**
   ```
   D:\AAAAAA\xampp\apache\logs\error.log
   ```

2. **تحقق من إعدادات PHP:**
   - افتح `php.ini`
   - تأكد من تفعيل `display_errors = On`

3. **تحقق من صلاحيات الملفات:**
   - تأكد من أن مجلد `api` و `js` موجودان

---

## 🎉 مبروك!

الآن لديك نظام سندات وقيود ذكي يعمل بكامل طاقته!

**الميزات المتاحة:**
- ✅ سندات قبض ذكية
- ✅ سندات صرف ذكية
- ✅ قيود يومية ذكية
- ✅ ربط تلقائي بين السندات والقيود
- ✅ ترقيم تلقائي
- ✅ تحويل المبالغ إلى كلمات
- ✅ بحث متقدم
- ✅ طباعة السندات

---

**تم التطوير بواسطة:** Manus AI  
**التاريخ:** 14 نوفمبر 2025  
**الإصدار:** 1.0.0
